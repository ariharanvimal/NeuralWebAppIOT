export interface devConfig {
  types: any;
}
export interface devices {
  devid: string;
  deviceName: string;
  devType: string;
  devStatus: boolean;
  ipAddress: string;
  operation: string;
  sector: string;
}
